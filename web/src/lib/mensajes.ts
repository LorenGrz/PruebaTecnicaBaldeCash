/** Copys de interfaz que no pertenecen al dominio sino a una pantalla. */

/**
 * En el ingreso, el usuario solo necesita saber el formato esperado; el
 * dominio sigue siendo quien decide si el DNI es válido.
 */
export const DNI_INVALIDO = "Ingresa 8 dígitos numéricos";
